%h1 Create a new Campaign
=form_for @campaign, builder: WithErrorFields, url: {controller: "ad_campaigns"}, html: {novalidate: true } do |f|
  =f.label :name, "Campaign Name"
  =f.text_field :name
  =f.label :default_bid
  =f.number_field :default_bid
  %p.muted
    %small This bid applies to the first ad group in this campaign, which you'll create in the next step.
  =f.submit "Save and Continue", class: 'btn btn-success'

  %a.btn.btn-warning{href: campaigns_path} Cancel New Campaign