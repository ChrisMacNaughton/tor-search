-unless notice.nil? && alert.nil?
  .row.span12
    - if flash[:alert]
      .alert
        %a.close{data: {dismiss: 'alert'}} &times;
        = flash[:alert]
    - if flash[:notice]
      .alert.alert-info
        %a.close{data: {dismiss: 'alert'}} &times;
        = flash[:notice]